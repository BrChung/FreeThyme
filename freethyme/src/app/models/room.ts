// For now we don't actually use this, but its probably a good idea to try and create a data model

export interface Room {
  userId: string;
}

// export interface Event {
//   startTime: timestamp;
//   endTime: timestamp;
// }
